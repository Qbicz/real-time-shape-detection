# Compile with:
g++ moments.cpp -Wall -Werror -std=c++11 `pkg-config --cflags --libs opencv` -o segm

# or
# with 'make moments'

# Run with (example)
./moments ../img/nuts.jpg

# or
# redirect compilation output to less:
less < <(make 2>&1)

